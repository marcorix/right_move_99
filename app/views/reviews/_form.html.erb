<%= simple_form_for [flat, review],
  data: {
    insert_reviews_target: "form",
    action: "insert-reviews#insert"
  } do |f|%>
  <%= f.input :rating, collection: (1..5) %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>
