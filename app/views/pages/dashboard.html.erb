<div class="container">
  <h1>Dashboard</h1>
  <%= render 'message_link' %>
  <ul>
    <% current_user.bookings.each do |booking| %>
      <li><p><%= booking.flat.name %></p> from: <%= booking.start_date.strftime("%A %d %b %Y") %> to: <%= booking.end_date.strftime("%A %d %b %Y") %></li>
    <% end %>
  </ul>
</div>
